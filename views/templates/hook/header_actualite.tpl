
        
        <style type="text/css">
        	{if !empty($ph_style.COLOR_PHACTU)}
        	#liste-left-phactu li p.title a {
        		color:{$ph_style.COLOR_PHACTU};
        	}
        	{/if}
        	
        	{if !empty($ph_style.COLOR_HV_PHACTU)}
        	#liste-left-phactu li p.title a:hover {
        		color:{$ph_style.COLOR_HV_PHACTU};
        	}
        	{/if}
        </style>